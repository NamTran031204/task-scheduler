# HOW TO USE docker-compose.yml TO BUILD AND RUN APPLICATION

## ONE TIME
### Build and Start
`docker-compose up --build`
### Check logs
`docker-compose logs -f`

## SPECIFICALLY
### Application Service
`docker-compose build task-scheduler`
### MySQL
`docker-compose build mysql`
